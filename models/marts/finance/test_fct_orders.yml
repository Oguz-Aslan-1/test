version: 2

models:
  - name: fct_orders
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: order_id
        tests:
          - unique
          - not_null